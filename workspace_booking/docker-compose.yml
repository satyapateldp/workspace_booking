services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: workspace_booking_web
    command: python manage.py runserver 0.0.0.0:8000
    volumes:
      - .:/app
    ports:
      - "8000:8000"
    env_file:
      - .env
